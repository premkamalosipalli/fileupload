version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:16
    working_directory: /home/circleci/file/upload
    steps:
      - checkout:
          path: /home/circleci/file/upload
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Build React App
          command: npm run build

  deploy:
    docker:
      - image: docker:19.03.12
    steps:
      - setup_remote_docker:
          version: 19.03.12
      - run:
          name: Build and Publish Docker Image
          command: |
            docker build -t D:\OAuth\fileupload .
            echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
            docker tag fileupload "$DOCKER_USERNAME/fileupload:latest"
            docker push "$DOCKER_USERNAME/fileupload:latest"

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
